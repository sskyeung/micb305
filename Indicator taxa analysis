```{r Alpha}
set.seed(421)
indval_alpha = multipatt( t(otu_table), 
                    cluster = ps_filt@sam_data$Alpha_carotene,
                    control = how(nperm = 999) )

summary(indval_alpha, indvalcomp = TRUE)

# Extract into table
indval_Alpha_table = as.data.frame(indval_alpha$sign)

ISA_Alpha = indval_Alpha_table %>% 
  filter( stat >=0.7, p.value <= 0.05 )
ISA_Alpha
```

```{r Beta}
indval_beta = multipatt( t(otu_table), 
                    cluster = ps_filt@sam_data$Beta_carotene,
                    control = how(nperm = 999) )

summary(indval_beta, indvalcomp = TRUE)

# Extract into table
indval_beta_table = as.data.frame(indval_beta$sign)

ISA_beta = indval_beta_table %>% 
  filter( stat >=0.7, p.value <= 0.05 )
ISA_beta
```

```{r Carotene total}
indval_total = multipatt( t(otu_table), 
                    cluster = ps_filt@sam_data$Carotene_total,
                    control = how(nperm = 999) )

summary(indval_total, indvalcomp = TRUE)

# Extract into table
indval_total_table = as.data.frame(indval_total$sign)

ISA_total = indval_total_table %>% 
  filter( stat >=0.7, p.value <= 0.05 )
ISA_total
```

```{r Copper}
indval_Copper = multipatt( t(otu_table), 
                    cluster = ps_filt@sam_data$Copper,
                    control = how(nperm = 999) )

summary(indval_Copper, indvalcomp = TRUE)

# Extract into table
indval_copper_table = as.data.frame(indval_Copper$sign)

ISA_copper = indval_copper_table %>% 
  filter( stat >=0.7, p.value <= 0.05 )
ISA_copper

```

```{r Manganese}
indval_mang = multipatt( t(otu_table), 
                    cluster = ps_filt@sam_data$Manganese,
                    control = how(nperm = 999) )

summary(indval_mang, indvalcomp = TRUE)

# Extract into table
indval_manganese_table = as.data.frame(indval_mang$sign)

ISA_manganese = indval_manganese_table %>% 
  filter( stat >=0.7, p.value <= 0.05 )
ISA_manganese
```

```{r Selenium}
indval_selenium = multipatt( t(otu_table), 
                    cluster = ps_filt@sam_data$Selenium,
                    control = how(nperm = 999) )

summary(indval_selenium, indvalcomp = TRUE)

# Extract into table
indval_sel_table = as.data.frame(indval_selenium$sign)

ISA_selenium = indval_sel_table %>% 
  filter( stat >=0.7, p.value <= 0.05 )
ISA_selenium
```

```{r Vitamin A}
indval_vit_A = multipatt( t(otu_table), 
                    cluster = ps_filt@sam_data$Vitamin_A,
                    control = how(nperm = 999) )

summary(indval_vit_A, indvalcomp = TRUE)

# Extract into table
indval_vit_A_table = as.data.frame(indval_vit_A$sign)

ISA_vit_A = indval_vit_A_table %>% 
  filter( stat >=0.7, p.value <= 0.05 )
ISA_vit_A
```

```{r Vitamin C}
indval_vit_C = multipatt( t(otu_table), 
                    cluster = ps_filt@sam_data$Vitamin_C,
                    control = how(nperm = 999) )

summary(indval_vit_C, indvalcomp = TRUE)

# Extract into table
indval_vit_C_table = as.data.frame(indval_vit_C$sign)

ISA_vit_C = indval_vit_C_table %>% 
  filter( stat >=0.7, p.value <= 0.05 )
ISA_vit_C
```

```{r Vitamin E}
indval_vit_E = multipatt( t(otu_table), 
                    cluster = ps_filt@sam_data$Vitamin_E,
                    control = how(nperm = 999) )

summary(indval_vit_E, indvalcomp = TRUE)

# Extract into table
indval_vit_E_table = as.data.frame(indval_vit_E$sign)

ISA_vit_E = indval_vit_E_table %>% 
  filter( stat >=0.7, p.value <= 0.05 )
ISA_vit_E
```
