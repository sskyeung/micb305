cd /work
mkdir project
cd project

# Import using manifest
qiime tools import \
  --type "SampleData[SequencesWithQuality]" \
  --input-format SingleEndFastqManifestPhred33V2 \
  --input-path /datasets/project_2/parkinsons/parkinsons_manifest.tsv \
  --output-path ./demux_seqs.qza

# Visualizing demultiplexed data
qiime demux summarize \
  --i-data demux_seqs.qza \
  --o-visualization demux.qzv

# Transfering file demux.qzv to local computer
scp root@10.34.36.95:/work/project/demux.qzv .

# Denoising
qiime dada2 denoise-single \
  --i-demultiplexed-seqs demux_seqs.qza \
  --p-trim-left 0 \
  --p-trunc-len 251 \
  --o-representative-sequences rep-seqs.qza \
  --o-table table.qza \
  --o-denoising-stats stats.qza

qiime metadata tabulate \
  --m-input-file stats.qza \
  --o-visualization stats.qzv

qiime feature-table summarize \
  --i-table table.qza \
  --o-visualization table.qzv \
  --m-sample-metadata-file /datasets/project_1/moving_pictures/sample-metadata.tsv
  
qiime feature-table tabulate-seqs \
  --i-data rep-seqs.qza \
  --o-visualization rep-seqs.qzv
